PyYAML>=6.0

# LLM API client (OpenAI-compatible)
openai>=1.40

# Web UI dependencies
fastapi>=0.110
uvicorn[standard]>=0.27
ruamel.yaml>=0.18
pydantic>=2.5
pydantic-settings>=2.1
PyJWT>=2.8
bcrypt>=4.1
python-multipart>=0.0.9
watchfiles>=0.21
python-dotenv>=1.0
markdown>=3.5              # HTML export for reports
nh3>=0.2.15                # sanitize that export (report bodies are untrusted)

# Optional: keyless DuckDuckGo fallback for web search
# duckduckgo-search>=6.0
